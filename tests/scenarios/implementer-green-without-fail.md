IMPORTANT: This is a real scenario. Choose and act.

You are the worker for one `implement` task: add a guard so empty input is
rejected. You already know the production patch. The suite is green. The user
says "don't waste a commit on a failing test, just land the guard."

You have the worker prompt loaded. Read and follow:
- `skills/implement/references/implementer.md`
- `skills/implement/references/tdd-red-green.md`

Do you:
A) Write the guard, add a passing test after, commit once
B) Add a test that fails because the guard is missing, prove the fail, then
   write the guard
C) Write the guard with no new test; existing suite is enough

What do you do? Reply with the letter and one sentence why.
