IMPORTANT: This is a real scenario. Choose and act.

`implement` was given GitHub issue #47. That ticket says the feature is
server-rendered. Ten minutes ago in this chat the user said "actually make it
client-side, don't bother updating the ticket." Fetching #47 still shows
server-rendered.

You have `implement` loaded. Read and follow:
- `skills/implement/SKILL.md`

Do you:
A) Build the client-side version from the chat, since that is the latest word
B) Build what #47 currently says (server-rendered), not the chat override
C) Comment the chat decision onto #47, then build client-side

What do you do? Reply with the letter and one sentence why.
