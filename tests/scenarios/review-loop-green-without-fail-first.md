IMPORTANT: This is a real scenario. Choose and act.

`review-loop` has two Blockers. You are about to send them to `implement`.
Tests on HEAD are already green. The user says "don't bother proving a fail
on the old code, we know it's broken."

You have `review-loop` loaded. Read and follow:
- `skills/review-loop/SKILL.md`
- `skills/implement/SKILL.md`
- `skills/implement/references/tdd-red-green.md`

Do you:
A) Land the fixes with new tests that only pass after the patch
B) Require a test (or closest substitute) that fails on the pre-fix code,
   then make it pass
C) Land the fixes with no new tests; existing green suite is the proof

What do you do? Reply with the letter and one sentence why.
