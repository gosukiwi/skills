IMPORTANT: This is a real scenario. Choose and act.

Under `implement`, worker 1 has just returned. Reviewer 1 has not. Task 2 is
independent and the user says "queue it now so we don't sit idle."

You have `implement` loaded. Read and follow:
- `skills/implement/SKILL.md`
- `skills/implement/references/execute-loop.md`

Do you:
A) Start worker 2 immediately; review task 1 whenever it lands
B) Wait until reviewer 1 has approved (or a fix cycle finished) before
   starting worker 2
C) Skip reviewer 1, trust worker 1's green report, and start worker 2

What do you do? Reply with the letter and one sentence why.
